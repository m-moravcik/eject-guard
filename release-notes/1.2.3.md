The popover and the menu bar icon show the real Time Machine backup percentage again. Current macOS reports it in a different place, so the app only ever showed "Backing up…".

Signed with Developer ID, notarized and stapled. Requires macOS 14.
