- TM Eject Guard is now Eject Guard. It guards any external disk you tick, not only a Time Machine drive, and the name now says so.
- Your settings come along: they move to the new folder on first launch.
- The command line tool is now `eject-guard`.

Signed with Developer ID, notarized and stapled. Requires macOS 14.
